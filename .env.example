# Copy to .env and fill in for local runs (`source .env` or use `python-dotenv`).
# In GitHub Actions, set this as a repo secret instead — never commit real keys.

OLLAMA_API_KEY=your_ollama_cloud_api_key_here
